#include<stdio.h>
#include<stdlib.h>
int main(){
   char subjects[20];
    printf("Enter the name of subjects:");
    for(int i=0;i<subjects;i++){
        scanf("%s",subjects[i]);
    }
    printf("The subjects are:");
    for(int i=0;i<subjects;i++){
        printf("The subjects are:",subjects[i];)
    }
}